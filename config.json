{   
    // path of data 数据的路径, 默认会选择 adult.data
    "dataPath": "data/adult.data",
    // 准标志符列的索引值
    "qiIndex": [0, 1, 4, 5, 6, 8, 9, 13],
    // 准标志符是否为字符串, 如果不是, 按照整数处理
    "isCat": [false, true, false, true, true, true, true, true],
    // 敏感信息索引值
    "saIndex" : -1
}